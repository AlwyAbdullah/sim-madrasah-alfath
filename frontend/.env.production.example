# ===== Frontend production =====
# Same-origin lewat Nginx: pakai path relatif (tanpa domain), aman untuk host apa pun.
# CATATAN: variabel NEXT_PUBLIC_* dibaca SAAT BUILD — set ini SEBELUM `npm run build`.
NEXT_PUBLIC_API_BASE=/api/v1
