# Server
APP_PORT=8080
APP_ENV=development

# CORS — origin frontend Next.js
CORS_ORIGIN=http://localhost:3000

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sim_madrasah

# JWT
JWT_SECRET=ganti-dengan-secret-acak-yang-panjang
JWT_EXPIRY_MINUTES=60

# Cookie (set true di produksi dengan HTTPS)
COOKIE_SECURE=false
